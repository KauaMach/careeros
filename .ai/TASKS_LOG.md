# 📓 Diário de Bordo & Tasks (Hand-off)

> **Regra:** Este arquivo deve ser atualizado pelo agente de IA no final de cada sessão/iteração longa. Ele serve para que o próximo agente saiba onde pegar a tarefa.

### Última Atualização: 23/07/2026 (Fim do MVP Core)

**✅ O que foi finalizado hoje:**
- Correções de Auth e Parsing (`[object Object]`, `passlib` -> `bcrypt`).
- **MVP 4:** Finalizado e antecipado `V1` e `V2` no editor de Currículos (Integração com Google Gemini AI e Exportação PDF).
- **MVP 5:** Criado módulo de Empresas (CRUD com tabelas `companies`, UI completa em `/companies`).
- **MVP 6:** Criado módulo de Certificados (CRUD com tabelas `certificates`, UI completa em `/certificates`).
- Reorganização da documentação (criação de `ARCHITECTURE.md` e `.ai/`).

**🚧 O que está pendente (Próximos Passos Imediatos):**
- Conectar a entidade `Company` no módulo de `Jobs` (permitir vincular uma empresa a uma vaga).
- **MVP 7:** Testes e Polish (Polimento). Revisar UI/UX de todas as telas construídas até aqui.
- Iniciar features de Dashboard Avançado (Gráficos) ou Kanban aprimorado.
